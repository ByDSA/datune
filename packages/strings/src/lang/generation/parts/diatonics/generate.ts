import Input from "./Input";

export default function generate(input: Input) {
  return {
    C: input.C,
    D: input.D,
    E: input.E,
    F: input.F,
    G: input.G,
    A: input.A,
    B: input.B,
  };
}
